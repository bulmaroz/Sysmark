<Window x:Class="SysMarkModerno.Views.UpcomingFollowUpsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:SysMarkModerno.Views"
        Title="Próximos Seguimientos (7 días y Atrasados)" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <Window.Resources>
        <local:DatePassedConverter x:Key="DatePassedConverter"/>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="CalendarCheck" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Seguimientos Pendientes" Style="{StaticResource MaterialDesignHeadline5TextBlock}" VerticalAlignment="Center"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="1" materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <ListView ItemsSource="{Binding UpcomingFollowUps}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <EventSetter Event="MouseDoubleClick" Handler="Item_MouseDoubleClick"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <!-- We set a default border and padding -->
                            <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}" Padding="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <!-- Si la fecha ya pasó, pintar de rojo claro -->
                                            <DataTrigger Binding="{Binding Seguimiento, Converter={StaticResource DatePassedConverter}}" Value="True">
                                                <Setter Property="Background" Value="#FFEBEE"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="{DynamicResource SecondaryHueMidBrush}" CornerRadius="20" Width="40" Height="40" Margin="0,0,16,0">
                                        <materialDesign:PackIcon Kind="ClipboardText" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="0,0,16,0">
                                        <TextBlock Text="{Binding Empresa}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{Binding Contacto}" Opacity="0.7"/>
                                        <TextBlock Text="{Binding Comentarios}" FontSize="12" Opacity="0.6" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="40"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Seguimiento, StringFormat={}{0:dd/MM/yyyy}}" FontWeight="Bold" HorizontalAlignment="Right">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Seguimiento, Converter={StaticResource DatePassedConverter}}" Value="True">
                                                            <Setter Property="Foreground" Value="#D32F2F"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Seguimiento, StringFormat={}{0:HH:mm}}" HorizontalAlignment="Right" Opacity="0.7"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Loading Overlay -->
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0" IsIndeterminate="True"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Width="48" Height="48" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Window>
